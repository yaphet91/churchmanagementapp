module.exports = {
    plugins: {
        'postcss-import': {},        // Using an object with an empty config
        'postcss-nested': {},        // Nesting plugin configuration
        'tailwindcss': {},           // Tailwind CSS configuration
        'autoprefixer': {},          // Autoprefixer configuration
    },
};
